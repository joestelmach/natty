<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.natty</groupId>
  <artifactId>parser</artifactId>
  <version>0.1-SNAPSHOT</version>
  
  <properties>
    <debug>false</debug>
    
    <project.build.sourceEncoding>
      UTF-8
    </project.build.sourceEncoding>

    <project.reporting.outputEncoding>
      UTF-8
    </project.reporting.outputEncoding>
  </properties>
  
  <build>
  	<plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>1.5</source>
          <target>1.5</target>
          <meminitial>128m</meminitial>
          <maxmem>512m</maxmem>
        </configuration>
      </plugin>
      
      <!-- while cleaning, remove any generated parser files -->
      <plugin>
		    <artifactId>maven-clean-plugin</artifactId>
		    <version>2.4.1</version>
		    <configuration>
		      <filesets>
		        <fileset>
		          <directory>src/main/java/com/natty/date/generated</directory>
		          <includes>
		            <include>**/*.java</include>
		          </includes>
		          <followSymlinks>false</followSymlinks>
		        </fileset>
		      </filesets>
		    </configuration>
		  </plugin>
      
      <plugin>
        <groupId>org.antlr</groupId>
        <artifactId>antlr3-maven-plugin</artifactId>
        <version>3.2</version>
        <executions>
        
          <!-- 
            execution for all grammars except DateParser, since these grammars
            should always be generated without debugging information
          -->
          <execution>
            <id>allButDateParser</id>
            <goals>
              <goal>antlr</goal>
            </goals>
            <configuration>
              <outputDirectory>src/main/java</outputDirectory>
              <debug>false</debug>
              <excludes>
                <exclude>com/natty/date/generated/DateParser.g</exclude> 
              </excludes>
            </configuration>
          </execution>
          
          <!-- 
            execution for just the DateParser, allowing the debug mode to 
            be set with the -Ddebug parameter 
          -->
          <execution>
            <id>dateParser</id>
            <goals>
              <goal>antlr</goal>
            </goals>
            <configuration>
              <outputDirectory>src/main/java</outputDirectory>
              <debug>${debug}</debug>
              <includes>
                <include>com/natty/date/generated/DateParser.g</include> 
              </includes>
            </configuration>
          </execution>
        </executions>
      </plugin>
      
      <plugin> 
		    <groupId>org.antlr</groupId> 
		    <artifactId>maven-gunit-plugin</artifactId> 
		    <version>3.2</version> 
		    <executions> 
		      <execution> 
		        <id>maven-gunit-plugin</id> 
		        <phase>test</phase> 
		        <goals> 
		          <goal>gunit</goal> 
		        </goals> 
		        <configuration>
              <skip>${debug}</skip>
            </configuration>
		      </execution> 
		    </executions> 
		  </plugin> 
      
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>2.2-beta-5</version>
        <configuration>
          <descriptors>
            <descriptor>src/main/assembly/cli.xml</descriptor>
          </descriptors>
          <archive>
            <manifest>
              <mainClass>${mainClass}</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>
    </plugins>
  </build>
  
  <dependencies>
    <dependency> 
      <groupId>org.antlr</groupId>
      <artifactId>antlr-runtime</artifactId>
      <version>3.2</version>
    </dependency> 
    
    <dependency> 
      <groupId>org.antlr</groupId>
      <artifactId>gunit</artifactId>
      <version>3.2</version>
    </dependency> 
    
    <dependency> 
      <groupId>antlr</groupId>
      <artifactId>antlr</artifactId>
      <version>2.7.7</version>
    </dependency> 
    
    <dependency>
      <groupId>org.json</groupId>
    	<artifactId>json</artifactId>
      <version>20090211</version>
    </dependency>
    
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.1</version>
    </dependency>
  </dependencies>
</project>
